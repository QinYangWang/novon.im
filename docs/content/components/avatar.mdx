---
title: Avatar
description: A user or entity image with a fallback.
icon: Users
order: 10
wide: true
---

import { Avatar, AvatarFallback, AvatarImage, Blobatar } from 'novon'

An avatar shows an image when it loads and falls back to initials or an icon when
it does not. The fallback is not an error state, it is part of the component.

## Usage

<Preview title="With a fallback" description="The initials show until the image resolves.">

<Cluster gap={12} align="center">
  <Avatar>
    <AvatarImage src="https://github.com/shadcn.png" alt="shadcn" />
    <AvatarFallback>SC</AvatarFallback>
  </Avatar>
  <Avatar>
    <AvatarImage src="/does-not-exist.png" alt="" />
    <AvatarFallback>NV</AvatarFallback>
  </Avatar>
</Cluster>

```mdx
<Avatar>
  <AvatarImage src="https://github.com/shadcn.png" alt="shadcn" />
  <AvatarFallback>SC</AvatarFallback>
</Avatar>
```

</Preview>

<Preview title="Blobatar fallback" description="A deterministic face for someone with no picture.">

<Cluster gap={12} align="center">
  <Avatar size={40}><Blobatar name="ada" fill alt="" /></Avatar>
  <Avatar size={40}><Blobatar name="grace" fill alt="" /></Avatar>
  <Avatar size={40}><Blobatar name="alan" fill alt="" /></Avatar>
</Cluster>

```mdx
import { Avatar, Blobatar } from 'novon'

<Avatar size={40}>
  <Blobatar name="ada" fill alt="" />
</Avatar>
```

</Preview>

<Preview title="Sizes" description="`size` sets the frame in pixels.">

<Cluster gap={12} align="center">
  <Avatar size={28}><AvatarFallback>SM</AvatarFallback></Avatar>
  <Avatar><AvatarFallback>MD</AvatarFallback></Avatar>
  <Avatar size={48}><AvatarFallback>LG</AvatarFallback></Avatar>
</Cluster>

```mdx
<Avatar size={28}><AvatarFallback>SM</AvatarFallback></Avatar>
<Avatar><AvatarFallback>MD</AvatarFallback></Avatar>
<Avatar size={48}><AvatarFallback>LG</AvatarFallback></Avatar>
```

</Preview>

## API

| Component | Element | Notes |
| --- | --- | --- |
| `Avatar` | `span` | The clipped, rounded frame. `size` sets its pixels; `xstyle` composes further styles. |
| `AvatarImage` | `img` | Falls back when it errors or is still loading. |
| `AvatarFallback` | `span` | Rendered whenever the image is not ready. |

Always give the image a meaningful `alt`, and mark it empty when the name is
already next to it so a screen reader does not repeat it.
