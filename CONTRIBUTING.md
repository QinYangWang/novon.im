# contributing to novon

感谢参与 novon。当前仓库处于 CLI 骨架阶段，优先保持公共命令契约稳定，并用小步、可验证的改动支持后续功能开发。

## 开始之前

- 使用 Node.js 18.18 或更高版本。
- 从仓库根目录执行 `npm ci` 安装依赖（当前没有运行时依赖）。
- 提交改动前执行 `npm run check`。

## 项目约定

- `src/cli.js` 是 npm `bin` 指向的 CLI 入口；公共命令名称集中定义在入口中。
- `test/cli.test.js` 使用 Node.js 内置测试运行器，覆盖命令帮助和基础错误行为。
- `npm run check` 是统一质量门槛，包含语法检查和自动化测试。
- 命令的业务实现应与公共入口分离，并保持 `init`、`dev`、`studio`、`build`、`publish` 之间共享的项目格式。
- 对外品牌始终写作小写 `novon`。日志或页面底部可以使用 `ฅ^•ﻌ^•ฅ`，不要把品牌写成首字母大写形式。

## 开发流程

1. 为一个明确的 issue 建立分支，避免把无关重构混入功能改动。
2. 先补充能复现需求的测试，再实现行为。
3. 更新用户可见行为对应的 README 或命令文档。
4. 本地运行 `npm run check`，并在 PR 描述中记录命令和结果。
5. PR 标题包含关联 issue 编号，说明变更摘要、验证方式和已知限制。

提交信息应简短、使用祈使语气，并聚焦单一变更。不要提交 `node_modules`、生成物或本地密钥。
